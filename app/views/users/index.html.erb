<% provide(:title, 'All Users') %>

<h1>All Users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
      <%=
      	render @users
      	# render user # first refactoring attempt at the index view
      %>
  <% end %>
</ul>

<%= will_paginate %>